#cloud-config
users:
  - name: ansible
    groups: sudo
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    ssh-authorized-keys:
      - ssh-rsa <pubkey here> 

#disable cloud-init on first boot
runcmd:
  - touch /etc/cloud/cloud-init.disabled
